#include <bits/stdc++.h>
using namespace std;

int main()
{
    
    int val;
    cin >> val;
    if (val < 0)
        cout << "invalid input";
    else if (val > 100)
        cout << "invalid input";
    else
        cout << val;
    return 0;
}
